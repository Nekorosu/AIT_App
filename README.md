# AIT_App — Электронный журнал (Avalonia + MySQL)

Миграция электронного журнала с WPF/MSSQL на Avalonia UI / MySQL / .NET 9.

---

## Быстрый старт

### 1. База данных
Импортируйте `mysql_schema.sql` на ваш MySQL-сервер (уже выполнено).

### 2. Строка подключения
Скопируйте `config.example.json` → `config.json` и заполните:
```json
{
  "ConnectionString": "Server=192.168.1.X;Port=3306;Database=electronic_journal;User=journal_user;Password=YOURPASSWORD;"
}
```
`config.json` находится в `.gitignore` — никогда не коммитится.

### 3. Запуск
```bash
cd AIT_App
dotnet run
```

### Учётные данные по умолчанию
| Логин | Пароль |
|-------|--------|
| teacher | 2222 |

---

## Структура файлов

```
AIT_App/
  Services/
    ConnectionStringService.cs   ← читает/пишет config.json
    DataBaseCon.cs               ← MySQL ADO.NET обёртка
    ExportService.cs             ← экспорт в Excel (ClosedXML)
  Windows/
    Frontend/                    ← AXAML разметка (дизайнер)
      AuthWindow.axaml
      HomeWindow.axaml
      SettingsWindow.axaml
      UserControls/
        Journal.axaml
        SessionReport.axaml
        PlanSession.axaml
    Backend/                     ← code-behind (кодер)
      AuthWindow/AuthWindow.axaml.cs
      HomeWindow/HomeWindow.axaml.cs
      SettingsWindow/SettingsWindow.axaml.cs
      UserControls/
        Journal/Journal.axaml.cs
        SessionReport/SessionReport.axaml.cs
        PlanSession/PlanSession.axaml.cs
```

---

## Функциональность

| Модуль | Что делает |
|--------|----------|
| **AuthWindow** | Асинхронная проверка соединения с БД при запуске, загрузка логинов из БД, аутентификация |
| **HomeWindow** | Навигационная оболочка (sidebar + ContentControl) |
| **Journal** | Журнал оценок — pivot-таблица (студенты × даты), добавление/удаление оценок, экспорт в Excel |
| **SessionReport** | Отчёт по сессии — pivot по студентам, таблица успеваемости (средний балл, качество, успеваемость %), экспорт в Excel |
| **PlanSession** | Управление плановыми сессиями (добавить/удалить), вызов `sp_UpdateGradeTypes` |
| **SettingsWindow** | Редактирование строки подключения, кнопка «Проверить соединение» |

---

## Известные задачи (технический долг)

- [ ] Хеширование паролей (сейчас plain text)
- [ ] Fade-анимация между окнами (WindowTransition)
- [x] Иконки `check.png` / `exclamation.png` — добавлены в папку `Icons/`
- [ ] Отличники / должники / кандидаты на красный диплом (фаза 9)


## Фаза 0 — GitHub и среда разработки

- [x] 0.1 — Создать репозиторий на GitHub
- [x] 0.2 — Добавить `.gitignore` (шаблон VisualStudio + дописать `.idea/`, `config.json`)
- [x] 0.3 — Выполнить `git rm -r --cached` для `bin/`, `obj/`, `.idea/` если они уже были закоммичены
- [x] 0.4 — Запушить проект, добавить остальных как Collaborators
- [x] 0.5 — Создать `config.example.json` с пустыми полями строки подключения и закоммитить его
- [x] 0.6 — Создать Issues на GitHub для всех задач фазы 1 и 2, назначить ответственных
- [ ] 0.7 — Включить GitHub Projects, создать kanban-доску (To do / In progress / Done)
- [ ] 0.8 — [Все] Сделать `git clone`, открыть проект в Rider, убедиться что компилируется
- [ ] 0.9 — [Все] Скопировать `config.example.json` → `config.json` (он в .gitignore, не коммитить)
- [ ] 0.10 — [Все] Изучить базовые команды Git: `pull`, `checkout -b`, `add`, `commit`, `push`

---

## Фаза 1 — База данных и сеть

- [x] 1.1 — [Хост-пк] Установить MySQL 8 на хост-компьютер
- [x] 1.2 — [Хост-пк] Создать пользователя БД (не root), выдать права только на базу `baselournalEl`
- [x] 1.3 — [Хост-пк] Накатить схему из `mysql_schema.sql` (таблицы, триггер, хранимая процедура)
- [x] 1.4 — [Хост-пк] Залить seed-данные: `INSERT INTO Данные_авторизации ('teacher', '2222', 0)`
- [x] 1.5 — [БД] Открыть порт 3306 в Firewall на хост-компьютере
- [x] 1.7 — [Тестирование] Проверить подключение с другого ПК через MySQL Workbench по IP лидера
- [x] 1.8 — [Все] Добавить NuGet-пакеты в `AIT_App.csproj`: `MySqlConnector 2.*`, `MsBox.Avalonia 3.*`, `ClosedXML 0.102.*`

---

## Фаза 2 — Фундамент кода
*Строго по порядку — каждый шаг зависит от предыдущего.*

### 2.1 ConnectionStringService
- [x] 2.1.1 — [Код] Создать `Services/ConnectionStringService.cs`
- [x] 2.1.2 — [Код] Реализовать чтение строки из `config.json` (System.Text.Json)
- [x] 2.1.3 — [Код] Реализовать запись строки в `config.json`
- [x] 2.1.4 — [Код] Если `config.json` не найден — возвращать дефолтную строку подключения

### 2.2 DataBaseCon
- [x] 2.2.1 — [Код] Создать `Services/DataBaseCon.cs`
- [x] 2.2.2 — [Код] Портировать `ExecuteQuery` с `SqlConnection` → `MySqlConnection`
- [x] 2.2.3 — [Код] Портировать `ExecuteScalar`
- [x] 2.2.4 — [Код] Портировать `ExecuteNonQuery` (обработка ошибки 1062 вместо 2627)
- [x] 2.2.5 — [Код] Портировать `ConnectionCheck`
- [x] 2.2.6 — [Код] Заменить все `[скобки]` на `` `бэктики` `` в SQL-запросах
- [x] 2.2.7 — [Код] Заменить `MessageBox.Show` на `MsBox.Avalonia` аналог

### 2.3 AuthWindow — логика
- [x] 2.3.1 — [Код] Добавить async проверку соединения при загрузке окна
- [x] 2.3.2 — [Код] Реализовать анимацию «Подключение...» через `DispatcherTimer`
- [x] 2.3.3 — [Код] Показывать иконку check.png / exclamation.png по результату проверки
- [x] 2.3.4 — [Код] Подключить кнопку «Вход» к запросу `SELECT` из `Данные_авторизации`
- [x] 2.3.5 — [Код] Блокировать кнопку «Вход» пока БД недоступна
- [x] 2.3.6 — [Код] При успешном входе открывать `HomeWindow`, закрывать `AuthWindow`
- [x] 2.3.7 — [Тестирование] Проверить: верный пароль, неверный пароль, БД недоступна

### 2.4 AuthWindow — разметка
- [x] 2.4.1 — [Дизайн] Убрать хардкод логина и пароля из ComboBox (оставить пустой выбор)
- [x] 2.4.2 — [Дизайн] Убедиться что поле пароля использует `PasswordChar="*"`
- [x] 2.4.3 — [Дизайн] Договориться с кодом об именах контролов (`x:Name`) до начала работы

---

## Фаза 3 — Навигация и оболочка

- [x] 3.1 — [Дизайн] Создать `Windows/Frontend/HomeWindow.axaml` — шапка + `ContentControl` по центру
- [x] 3.2 — [Код] Создать `Windows/Backend/HomeWindow/HomeWindow.axaml.cs`
- [x] 3.3 — [Код] Реализовать навигацию: кнопки меняют `Content` у `ContentControl`
- [x] 3.4 — [Код] Передавать данные авторизованного пользователя в HomeWindow (имя, роль)
- [x] 3.5 — [Тестирование] Проверить переключение между разделами, убедиться что старый контрол выгружается

---

## Фаза 4 — UserControl: PlanSession (планировщик сессий)

- [x] 4.1 — [Дизайн] Создать разметку `PlanSession.axaml` — таблица + форма добавления
- [x] 4.2 — [Код] При загрузке заполнять ComboBox из `SELECT Название FROM Дисциплины`
- [x] 4.3 — [Код] При загрузке заполнять DataGrid из `SELECT * FROM Запланированные_сессии`
- [x] 4.4 — [Код] Реализовать добавление записи (`INSERT INTO Запланированные_сессии`)
- [x] 4.5 — [Код] Реализовать удаление записи (`DELETE FROM Запланированные_сессии WHERE ID = @ID`)
- [x] 4.6 — [Тестирование] Проверить добавление, удаление, дублирование записи, пустые поля

---

## Фаза 5 — UserControl: SessionReport (просмотр сессий)
*Самый сложный модуль — pivot-запрос, два DataGrid.*

- [x] 5.1 — [Дизайн] Доработать разметку `SessionReport.axaml` — два DataGrid в правой колонке
- [x] 5.2 — [код] При загрузке заполнять ComboBox групп из `SELECT \`Название группы\` FROM Группы`
- [x] 5.3 — [код] Портировать pivot-запрос с MSSQL на MySQL (заменить `[скобки]` на бэктики)
- [x] 5.4 — [код] Исправить SQL-инъекцию: `@groupName` параметром, названия предметов валидировать через список из БД
- [x] 5.5 — [код] Реализовать динамическое построение колонок DataGrid в Avalonia
- [x] 5.6 — [код] Реализовать расчёт качественной успеваемости (`QualityAcademicPerformance`)
- [x] 5.7 — [код] Реализовать список студентов по оценке (двоечники / троечники / хорошисты / отличники)
- [x] 5.8 — [код] Заменить `WPF DataGridTextColumn` + `Binding` на Avalonia-аналоги
- [x] 5.9 — [Тестирование] Проверить: группа без оценок, группа с частичными оценками, все оценки заполнены

---

## Фаза 6 — UserControl: Journal (журнал оценок)
*В оригинале пустой стаб — пишется с нуля.*

- [x] 6.1 — [Код] Договориться о функциональности: что именно показывает журнал
- [x] 6.2 — [Дизайн] Создать разметку `Journal.axaml`
- [x] 6.3 — [код] Реализовать загрузку оценок по выбранной группе и предмету
- [x] 6.4 — [код] Реализовать добавление / редактирование оценки (`INSERT/UPDATE Аттестация`)
- [x] 6.5 — [Тестирование] Проверить триггер: оценка на дату сессии получает тип «Экзаменационная»

---

## Фаза 7 — SettingsWindow (настройка подключения)

- [x] 7.1 — [Дизайн] Создать разметку `SettingsWindow.axaml` — поле ввода строки + кнопки «Сохранить» / «Проверить»
- [x] 7.2 — [код] Подключить к `ConnectionStringService` — загружать текущую строку, сохранять новую
- [x] 7.3 — [код] Кнопка «Проверить» — вызывает `ConnectionCheck`, показывает результат
- [x] 7.4 — [Тестирование] Проверить сохранение, проверить что после рестарта приложения новая строка используется

---

## Фаза 8 — Excel-экспорт

- [x] 8.1 — [код] Создать `Services/ExportService.cs`
- [x] 8.2 — [код] Реализовать экспорт DataTable в `.xlsx` через ClosedXML
- [x] 8.3 — [код] Добавить кнопку «Экспорт» в `SessionReport`
- [x] 8.4 — [код] Добавить кнопку «Экспорт» в `Journal`
- [x] 8.5 — [Тестирование] Проверить открытие файла в Excel / LibreOffice, кириллица в заголовках

---

## Фаза 9 — Дополнительные отчёты (если останется время)

- [ ] 9.1 — Отчёт по успеваемости: средний балл, процент сдавших, разбивка по предметам
- [ ] 9.2 — Список отличников (все оценки 5)
- [ ] 9.3 — Кандидаты на красный диплом
- [ ] 9.4 — Список должников (есть незакрытые экзамены)

---

## Технический долг — исправить в процессе

- [ ] TD-1 — Пароли в `Данные_авторизации` хранятся открытым текстом — добавить хеширование (хотя бы SHA-256)
- [x] TD-2 — Убрать хардкод имён пользователей из `AuthWindow.axaml` ComboBox (логины загружаются из БД)
- [x] TD-3 — Все SQL-запросы используют параметризованные запросы, SQL-инъекции исключены
- [ ] TD-4 — `WindowTransition` (fade-анимация) — портировать на Avalonia.Animation или убрать

---